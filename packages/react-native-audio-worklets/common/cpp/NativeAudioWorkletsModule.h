#pragma once

// Autolinking includes `<NativeAudioWorkletsModule.h>` (see cxxModuleHeaderName in
// react-native.config.js). The implementation lives under audioworklets/.
#include <audioworklets/NativeAudioWorkletsModule.h>
