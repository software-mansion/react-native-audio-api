---
sidebar_position: 1
---

# AudioContextOptions

`AudioContextOptions` is a dictionary object specifies sample rate o use for the new context.

```jsx
interface AudioContextOptions {
  sampleRate: number;
}
```
