# Maintenance — expressive-code

> Used by `/pre-push-update` only — not loaded when the `expressive-code` skill is active.

Review this skill when `pre-push-update` reports changes in:

| Path | What to check |
|---|---|
| `.claude/skills/expressive-code/SKILL.md` | Principles, anti-patterns, trigger phrases |
| `CLAUDE.md` | Skills table row |
| `.claude/README.md` | Skills tree lists `expressive-code/` |
