# Maintenance — code-comments

> Used by `/pre-push-update` only — not loaded when the `code-comments` skill is active.

Review this skill when `pre-push-update` reports changes in:

| Path | What to check |
|---|---|
| `.claude/skills/code-comments/SKILL.md` | Hard rules, when-to / when-not tables, anti-patterns, trigger phrases |
| `CLAUDE.md` | Skills table row; short Comment style bullets stay accurate |
| `.claude/README.md` | Skills tree lists `code-comments/` |
| `CONTRIBUTING.md` | Pointer to comment guidelines, if one is added |
